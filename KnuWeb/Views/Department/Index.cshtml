@model IEnumerable<EmployeeEF.DEPARTMENT>

@{
    ViewBag.Title = "Факультети";
}
@Styles.Render("~/Content/Styles/TableStyleSheet.css")
<h2>Факультети</h2>

<p>
    @Html.ActionLink("Створити новий", "Create")
</p>
<table class="table myTable">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.D_NAME)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.D_NAME)
            </td>
            <td>
                @Html.ActionLink("Редагувати", "Edit", new { id = item.ID }) |
                @Html.ActionLink("Видалити", "Delete", new { id = item.ID })
            </td>
        </tr>
    }

</table>
